resources:
  - ../../base
  - namespace.yaml

nameSuffix: -dev
namespace: dev

patches:
  - path: kafka-patch.yaml
    target:
      kind: Deployment
      name: kafka
  - path: schema-registry-patch.yaml
    target:
      kind: Deployment
      name: schema-registry